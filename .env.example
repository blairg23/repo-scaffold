# Copy to .env and set real values.
# Keep .env uncommitted.

# GitHub token used by repo creation/settings/backlog scripts.
# Recommended classic PAT scopes: repo, workflow, read:org.
# Optional scopes: delete_repo (auto-cleanup), project (if you automate Projects).
GH_TOKEN=ghp_replace_with_real_token

# Optional metadata for local docs/tooling.
GITHUB_ORG=YOUR_ORG
GITHUB_REPO=YOUR_REPO
# Alternative single value:
# GH_REPO=YOUR_ORG/YOUR_REPO

# Optional backlog project naming defaults (used with --with-project):
# GITHUB_PROJECT_TITLE=YOUR_FIXED_PROJECT_TITLE
# GITHUB_PROJECT_TITLE_TEMPLATE={repo} Roadmap

# Test-only GitHub E2E toggles:
# RUN_GITHUB_E2E=1
# GITHUB_E2E_REPO_BASENAME=repo-scaffold-e2e
# GITHUB_E2E_VISIBILITY=public
# GITHUB_E2E_KEEP_REPO=0
# GITHUB_E2E_SKIP_SETTINGS_ASSERTS=0

# Legacy lowercase aliases are still supported:
# github_token=...
# github_org=...
# github_repo=...
# github_full_repo=...
# github_project_title=...
# github_project_title_template=...
